Adds two metadata properties, loadBefore and loadAfter for mods to specify other mods (by
id). Queue Wizard will attempt to order them appropriately, as well as wait for them to
initialize. Supports wildcard \*.
